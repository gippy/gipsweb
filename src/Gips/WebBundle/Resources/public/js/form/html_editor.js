// Generated by CoffeeScript 1.6.3
var editors, setupEditor;

setupEditor = function($editorDiv) {
  var editor, editorID, inputID;
  editorID = $editorDiv.attr('id');
  inputID = editorID.slice(0, editorID.length - 7);
  editor = ace.edit(editorID);
  $editorDiv.css('fontSize', '14px');
  editor.getSession().setUseWorker(false);
  editor.setTheme("ace/theme/idle_fingers");
  editor.getSession().setTabSize(4);
  editor.getSession().setUseSoftTabs(true);
  return editor.getSession().on('change', function(e) {
    return $("#" + inputID).val(editor.getSession().getDocument().getAllLines().join('\n'));
  });
};

editors = $('.ace-editor');

if (editors.length) {
  editors.each(function() {
    return setupEditor($(this));
  });
}

/*
//@ sourceMappingURL=html_editor.map
*/
